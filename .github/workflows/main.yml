name: Deploy Website

on:
  push:
    branches:
      - main  # jab main branch pe code push karo to chale

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # Agar tumhe npm se JS build ya tailwind build karna ho to add karo
      # - name: Setup Node.js
      #   uses: actions/setup-node@v3
      #   with:
      #     node-version: 18
      #
      # - run: npm install
      # - run: npm run build

      - name: Deploy to Server
        uses: easingthemes/ssh-deploy@v2.0.7
        with:
          ssh-private-key: ${{ secrets.SERVER_SSH_KEY }}
          remote-user: your_server_user
          server-ip: your_server_ip
          remote-path: /var/www/your-website
